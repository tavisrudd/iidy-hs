I noticed that our workplan and sub docs don't have much research and
code pointers in them. each phase should begin with a research cycle
that builds up important context for the implementation step with
pointers to code, sample snippets etc. that should be added into the
phase document and committed, then claude should begin implementing
the phase. don't begin implementing if the docs doesn't have the
research in it. see @WORKPLAN.md and the docs it references. update
workplan or @CLAUDE.md with that instruction. This helps avoid us
having to redo research each time through the ralph loop. use the file
system in the repo to store research, don't rely on your context
window and external memory files. if your memory files are full of
code research, move them into files in notes/ that we commit along with code.

